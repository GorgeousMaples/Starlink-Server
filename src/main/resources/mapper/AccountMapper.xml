<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.mapper.AccountMapper">

    <resultMap id="AccountVoResultMap" type="com.app.domain.vo.AccountVo">
        <id column="C_ID" property="id"/>
        <result column="C_USER_ID" property="userId"/>
        <result column="C_TYPE" property="type"/>
        <result column="C_NAME" property="name"/>
        <result column="C_PASSWORD" property="password"/>
        <result column="C_PHONE" property="phone"/>
        <result column="C_EMAIL" property="email"/>
    </resultMap>

    <select id="selectVoList" resultMap="AccountVoResultMap">
        select t_account.*,
               t_user.C_NAME, t_user.C_PASSWORD, t_user.C_PHONE, t_user.C_EMAIL
        from t_account
                 left join t_user on t_account.C_USER_ID = t_user.C_ID
    </select>

    <select id="selectVoById" resultMap="AccountVoResultMap">
        select t_account.*,
               t_user.C_NAME, t_user.C_PASSWORD, t_user.C_PHONE, t_user.C_EMAIL
        from t_account
                 left join t_user on t_account.C_USER_ID = t_user.C_ID
        where t_account.C_ID = #{id}
    </select>
</mapper>